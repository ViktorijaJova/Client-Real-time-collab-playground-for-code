// src/components/SessionDetails.tsx

export {}; // This makes the file a module

const SessionDetails: React.FC = () => {
    return (
        <div>
            <h2>Session Details</h2>
            {/* Add your session details implementation here */}
        </div>
    );
};

export default SessionDetails;
