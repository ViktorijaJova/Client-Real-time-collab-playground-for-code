// src/components/SessionList.tsx

export {}; // This makes the file a module

const SessionList: React.FC = () => {
    return (
        <div>
            <h2>Session List</h2>
            {/* Add your session list implementation here */}
        </div>
    );
};

export default SessionList;
